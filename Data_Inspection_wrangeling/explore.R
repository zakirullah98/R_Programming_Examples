#It loads the dataset mtcars into  R environment
#data(mtcars)
#head(mtcars)

#shape

#dim(mtcars)
#nrow(mtcars)
#ncol(mtcars)

head(iris)


#inspect dat types
str(iris)
#min, max, mean, etc.
summary(iris)
summary(iris$Species)



